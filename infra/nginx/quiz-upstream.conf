# /etc/nginx/conf.d/quiz-upstream.conf — 활성 색의 유일한 진실원
# 배포 스크립트(.github/workflows/deploy.yml 5단계)가 매 배포마다 이 파일을 통째로 다시 쓴다.
# 손으로 고칠 일은 runbook §2 수동 롤백뿐이다. blue=8092, green=8093.
upstream quiz_backend {
    server 127.0.0.1:8093;   # active: green
}
